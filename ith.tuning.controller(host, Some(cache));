host:     let start = str|host: &str| with.tuning.controller(host, Some(cache));
host:     let start = str|host: &str| with.tuning.controller(host, Some(cache));
host:     let start = str|host: &str| with.tuning.controller(host, with.cache);
